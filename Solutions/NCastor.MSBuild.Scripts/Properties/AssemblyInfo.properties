<?xml version="1.0" encoding="utf-8" ?>

<Project DefaultTargets="All" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <!-- Assembly info files -->
  <PropertyGroup>
    <AssemblyVersionFilePath Condition="$(AssemblyVersionFilePath) == ''">$(AssemblyInfoPath)\AssemblyVersion.cs</AssemblyVersionFilePath>
    <AssemblyCommonInfo Condition="$(AssemblyCommonInfo) == ''">$(AssemblyInfoPath)\AssemblyCommonInfo.cs</AssemblyCommonInfo>
  </PropertyGroup>

</Project>
