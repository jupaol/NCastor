<?xml version="1.0" encoding="utf-8" ?>

<!--
<copyright file="RunMSTestWithOpenCover.target" company="Juan Pablo Olmos Lara (Jupaol)">

  jupaol@hotmail.com
  http://jupaol.blogspot.com/

</copyright>
-->

<Project DefaultTargets="All" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <RunMSTestWithOpenCoverDependsOn>
      SettingMSTestTestingAssemblies;
      SettingInstrumentationAssemblies;
      ValidateMSTestProperties;
      ValidateOpenCoverProperties;
      CleanMSTestArtifactDirectories;
      CreateMSTestArtifactDirectories;
      SetMSTestsWithOpenCoverArguments;
      CustomizeMSTestWithOpenCoverArguments;
      BeforeRunMSTestWithOpenCover;
      CoreRunMSTestWithOpenCover;
      AfterRunMSTestWithOpenCover;
      CreateMSTestReport;
    </RunMSTestWithOpenCoverDependsOn>
  </PropertyGroup>

  <Target Name="RunMSTestWithOpenCover" DependsOnTargets="$(RunMSTestWithOpenCoverDependsOn)"/>

  <!-- To be overriden by the user -->
  <Target Name="BeforeRunMSTestWithOpenCover" />
  <Target Name="AfterRunMSTestWithOpenCover" />

  <Target Name="CoreRunMSTestWithOpenCover" >

    <Message Text="$(LoggingMessagePrefix) Running all the MSTest unit tests specified in the (MSTestContainers) item using OpenCover..." Importance="high" />

    <Error ContinueOnError="false" Text="The OpenCover MSTest results file path was not specified" Condition="$(OpenCoverMSTest_ReportFilePath) == ''" />
    <Error Text="The OpenCover runner arguments to be used with MSTest were not specified" Condition="$(OpenCoverRunnerArgumentsForMSTest) == ''" />

    <Delete Files="$(MSTestReportFilePath)" Condition="Exists($(MSTestReportFilePath))" />

    <Message Text="$(LoggingMessagePrefix) Running the MSTest unit tests with OpenCover..." Importance="normal" />
    <Exec ContinueOnError="true" Command='"$(OpenCoverRunner)" $(OpenCoverRunnerArgumentsForMSTest)'>
      <Output TaskParameter="exitcode" ItemName="ErrorsOcurredWhenRunningMSTestTests"/>
    </Exec>

  </Target>
  
</Project>
